require "./app"
require "sinatra/activerecord/rake"

# Основные команды:

# 1. rake db:create_migration NAME=name_of_migration - создаёт новую миграцию в db/migrate/
# 2. rake db:migrate - применяет(выполняет) созданную миграцию
# 3. rake db:rollback - возврат к предыдущей миграции